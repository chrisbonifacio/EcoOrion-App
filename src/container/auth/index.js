import React from 'react';
import {connect} from 'react-redux';

import {AmplifyAuthState} from '../../redux/constants';

const mapStateToProps = state => ({
  authState: state.authState,
});

const Auth = ({children}) => {
  return (
    
  )
}